# STAR TEAM — Python 의존성
# 설치: pip install -r requirements.txt
# Playwright 브라우저 설치: python -m playwright install chromium

# ── 테스트 ──────────────────────────────────────────────────────
pytest==9.0.3
pytest-playwright==0.7.2      # Playwright Python 바인딩 + pytest 통합
playwright==1.59.0

# ── Supabase ────────────────────────────────────────────────────
supabase==2.30.0               # Supabase Python 클라이언트
python-dotenv==1.2.1           # .env 파일 로드

# ── 모니터링 ────────────────────────────────────────────────────
sentry-sdk==2.59.0             # Sentry 에러 트래킹

# ── 유틸 ────────────────────────────────────────────────────────
requests==2.32.3               # Vercel REST API 호출
